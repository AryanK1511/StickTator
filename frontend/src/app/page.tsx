import { FC } from "react";
import { LandingPage } from "@/components";

const Home: FC = () => {
    return (
        <div>
            <LandingPage />
        </div>
    );
};

export default Home;
